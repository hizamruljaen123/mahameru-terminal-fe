import OilRefineryPanel from './OilRefineryPanel';

// For now, OilFacilityPanel acts as the primary global entry for refinery nodes
// with potential for broader facility types in future updates.

export default function OilFacilityPanel() {
  return <OilRefineryPanel />;
}
